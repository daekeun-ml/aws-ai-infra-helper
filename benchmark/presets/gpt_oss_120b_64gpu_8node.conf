# GPT-OSS 120B (MoE) | 64 GPU (8 nodes) | bf16 only
# DP = 64 / (1*1*1) = 64, EP=64=DP → pure_DP=1 (no data replication)
# GA = GBS / (MBS * DP) = 128 / (2 * 64) = 1
# 주의: 사용자 요청 GBS=120은 120/(2*64)=0.9 (비정수) → GBS=128로 조정
# 사용자 요청 GA=10: DP=64 기준 불가. EP를 줄이면 가능하나 ref spec 유지
GBS=128
MBS=2
TP=1
PP=1
CP=1
VP=     # PP=1이므로 VP 불필요
EP=64
FSDP=0
