# Qwen3-30B-A3B | 8 GPU (1 node) | B200 / GB200
# DP = world_size / (TP * PP * EP) = 8 / (1*1*8) = 1
# GA = GBS / (MBS * DP) = 512 / (1*1) = 512... wait, 64 as specified
# GBS=512, MBS=1, GA=64 → effective DP=8 (GBS = MBS * GA * DP = 1*64*8 = 512 ✓)
GBS=512
MBS=1
TP=1
PP=1
CP=1
VP=1   # PP=1이면 VP=1로 명시 (base config의 VP=12 오버라이드 필수)
EP=8
FSDP=0
# SEQ_LEN=4096  # Qwen3 config에 하드코딩됨, 변경 불필요
