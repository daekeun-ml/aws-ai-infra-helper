# Qwen3-VL 30B A3B (VLM SFT) | 8 GPU (1 node) | bf16 only
# TP=1, PP=1, EP=8: world_size=8, DP=8, EP=8 (no data replication per expert group)
# GA = GBS / (MBS * DP) = 32 / (1 * 8) = 4
# TASK=sft, DOMAIN=vlm (set automatically by model selection in the script)
GBS=32
MBS=1
TP=1
PP=1
CP=1
VP=
EP=8
FSDP=0
NO_CUDA_GRAPHS=1    # Qwen3-VL: mRoPE 가변 시퀀스 길이로 CUDA graph 비호환 → 반드시 비활성화
GA=                 # 설정 시 GBS 자동 역산: GBS = MBS * GA * DP. 비워두면 GBS 직접 사용
